import { mockTeam } from "@shared/mock/mock-team";

export const generateOptimalTeam = async (
  playerData: any,
  preferences: any
): Promise<any> => {
  // call datascience server
  const optimalTeam = {};

  return mockTeam;
};
