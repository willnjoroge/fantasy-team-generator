import { Player, Team } from "@shared/models/Player";
import { mockTeam } from "@shared/mock/mock-team";

export const generateBestTeam = (players: Player[], budget: number): Team => {
  return mockTeam;
};
